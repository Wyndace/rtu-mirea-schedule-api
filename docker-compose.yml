services:
  api:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - DEBUG=${DEBUG:-false}
      - PORT=${PORT:-8000}
      - CACHE_TTL=${CACHE_TTL:-3600}
    restart: unless-stopped
